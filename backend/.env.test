NODE_ENV=test
PORT=4000
DATABASE_URL=postgres://asean_go:asean_go_dev@localhost:5433/asean_go_test
JWT_SECRET=test-jwt-secret-not-for-production-use-only
JWT_EXPIRES_IN=7d

# Omise sandbox (test mode) keys — safe to commit, these can only touch
# Omise's simulated test-mode ledger, never real money.
OMISE_PUBLIC_KEY=pkey_test_68i47yl4esff7r9r0q4
OMISE_SECRET_KEY=skey_test_68i47ymbnyz5z5syzyw
